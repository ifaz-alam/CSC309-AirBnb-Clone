from rest_framework.response import Response

def updateProperty(request):
    #use request.user to get the user. 
    pass
    #if request.user != property.owner:
        #Repsonse (error your are not the owner) 